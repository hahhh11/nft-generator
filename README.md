# NFT 头像生成器 (Docker 部署版)

一个基于网页的 NFT 头像生成器，支持自定义图层和资源，可通过 Docker 快速部署。

## 功能特性

- 🎨 **自定义图层**：支持上传文件或路径拼接两种方式加载资源
- 🔄 **随机生成**：一键生成随机组合的 NFT 头像
- 📥 **生成所有**：批量生成所有可能的组合并打包下载
- ⚙️ **尺寸设置**：支持自定义生成图片的尺寸
- 📱 **响应式设计**：适配不同屏幕尺寸
- 🐳 **Docker 支持**：可通过 Docker 快速部署

## Docker 部署方法

### 方法一：使用 Docker 命令

1. **克隆或下载项目**
   ```bash
   git clone https://github.com/your-repo/nft-generator.git
   cd nft-generator
   ```

2. **构建 Docker 镜像**
   ```bash
   docker build -t nft-generator .
   ```

3. **运行 Docker 容器**
   ```bash
   docker run -d -p 6877:80 --name nft-generator --restart unless-stopped nft-generator
   ```

4. **访问应用**
   在浏览器中访问 `http://localhost:6877`

### 方法二：使用 Docker Compose

1. **克隆或下载项目**
   ```bash
   git clone https://github.com/your-repo/nft-generator.git
   cd nft-generator
   ```

2. **启动服务**
   ```bash
   docker-compose up -d
   ```

3. **访问应用**
   在浏览器中访问 `http://localhost:6877`

### 停止和删除容器

- **停止容器**
  ```bash
  # 使用 Docker 命令
  docker stop nft-generator
  
  # 使用 Docker Compose
  docker-compose down
  ```

- **删除容器**
  ```bash
  # 使用 Docker 命令
  docker rm nft-generator
  ```

- **删除镜像**
  ```bash
  docker rmi nft-generator
  ```

## 使用说明

1. **定义图层和资源**
   - 选择加载方式：上传文件或路径拼接
   - 添加图层，为每个图层命名
   - 上传图片文件或填写特征列表

2. **设置图片尺寸**
   - 点击右上角的设置按钮
   - 输入自定义宽度和高度
   - 点击"应用尺寸"

3. **生成 NFT**
   - 点击"随机生成头像"生成单个 NFT
   - 点击"生成所有并下载"生成所有组合并打包下载

4. **下载 NFT**
   - 点击"下载 PNG"下载当前显示的 NFT
   - 生成所有组合后会自动下载 zip 文件

## 项目结构

```
nft-generator/
├── index.html          # 主页面
├── style.css           # 样式文件
├── generator.js        # 核心逻辑
├── Dockerfile          # Docker 构建文件
├── docker-compose.yml  # Docker Compose 配置
└── README.md           # 使用说明
```

## 技术栈

- **前端**：HTML5, CSS3, JavaScript
- **Docker**：Nginx 作为 Web 服务器

## 系统要求

- Docker 18.06.0+ 
- Docker Compose 1.22.0+ (可选)
- 至少 512MB 内存
- 至少 100MB 磁盘空间

## 开发说明

如果您想修改或扩展项目，可以直接编辑源代码，然后重新构建 Docker 镜像。

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！
